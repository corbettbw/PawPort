<!DOCTYPE html>
<html>
  <head>
    <title>PawPort</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="min-h-screen bg-gray-50 text-gray-900 font-sans">
    <header class="p-4 flex justify-between items-center bg-white shadow-sm">
      <%= render "shared/flash" %>
      <h1 class="text-xl font-bold text-rose-600">🐾 PawPort</h1>

      <% if user_signed_in? %>
        <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm font-medium text-red-600 hover:text-red-800" %>
      <% else %>
        <div class="space-x-3">
          <%= link_to "Sign in", new_user_session_path, class: "text-sm font-medium text-blue-600 hover:text-blue-800" %>
          <%= link_to "Sign up", new_user_registration_path, class: "text-sm font-medium text-blue-600 hover:text-blue-800" %>
        </div>
      <% end %>
    </header>

    <main class="max-w-5xl mx-auto py-10 px-4">
      <%= yield %>
    </main>
  </body>
</html>
