<div class="mt-10">
  <%= render "shelters/shared/public_shelter_profile", shelter: @shelter %>
</div>

<% if @is_member %>
  <div class="max-w-6xl mx-auto mt-8 md:flex md:space-x-6">
    <!-- Sidebar (empty for now – Component 2) -->
    <aside class="w-full md:w-64 mb-6 md:mb-0">
      <div class="bg-slate-100 rounded-2xl p-4 text-slate-500 text-sm">
        Sidebar placeholder
      </div>
    </aside>

    <!-- Main control-panel content (empty for now) -->
    <main class="flex-1">
      <div class="bg-white rounded-2xl shadow-sm p-6 text-slate-500">
        Control panel content will appear here after sidebar + tabs are added.
      </div>
    </main>
  </div>
<% end %>
